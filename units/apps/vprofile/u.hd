
deploy()
{
	_rh_try_ret_help 'Deploy supabase with default configs'
	hd_env_apply_config deploy.yml
	if [ -z "$HOST_BASE" ]
	then
		echo "HOST_BASE not set, skip add ingress."
	else
		HOST="app.${HOST_BASE}" SERVICE=vprofile-app-service PORT=8080 hammerhand u ingress/traefik ing_srv_short
	fi
}
